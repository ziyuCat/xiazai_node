# 万能下载器测试计划

## 测试目标

确认三件事：

1. 分享文案能稳定解析
2. 结果页能稳定展示
3. 视频或图片能真正保存成功

## 测试图谱

| 流程 | 分支 | 应覆盖测试 |
|---|---|---|
| 输入分享文案 | 手输 / 粘贴 / 剪贴板自动识别 | 小程序单测 + 真机手测 |
| 解析接口 | 合法抖音文案 / 无链接 / 多链接 / 失效短链 | API 集成测试 |
| 短链展开 | 301 / 302 / 多跳转 / 超时 | API 集成测试 |
| 元数据渲染 | 视频 / 图集 / 缺作者头像 / 无大小信息 | 页面渲染测试 |
| 下载准备 | 正常票据 / 过期票据 / 非法 sourceId | API 集成测试 |
| 文件下载 | 成功 / 超时 / 网络断开 / 上游 403 | 真机手测 + 回归测试 |
| 保存相册 | 已授权 / 首次授权 / 拒绝授权 | 真机手测 |
| 历史记录 | 首次为空 / 有记录 / 删除后为空 | 页面测试 |
| 失败反馈 | 解析失败上报 / 下载失败上报 | API 集成测试 |

## 最重要的 12 条用例

1. 粘贴标准抖音分享文案，3 秒内返回标题、封面、作者、下载源
2. 粘贴没有 URL 的文本，前端直接拦截并提示
3. 粘贴多个 URL，后端只识别目标平台第一个有效作品链接
4. 短链跳转超过预设次数时，后端返回可读错误
5. 作品已删除或私密时，结果页不展示假数据
6. 点击“保存视频”后，能看到下载中状态和最终成功反馈
7. 用户拒绝相册权限时，出现明确引导
8. 下载票据过期后再次点击保存，会自动重新申请票据
9. 同一作品二次解析命中缓存，明显快于首次
10. 同一 OpenID 高频请求时被限流，但普通用户不受影响
11. 上游下载源失效时，系统记录失败样本
12. 弱网环境下不会一直无响应，必须超时并可重试

## 监控指标

上线第一周至少盯住这些数字：

1. `parse_success_rate`
2. `download_prepare_success_rate`
3. `save_success_rate`
4. `p95_parse_latency`
5. `p95_download_prepare_latency`
6. `invalid_link_ratio`
7. `upstream_block_ratio`

## 上线前人工回归

1. 安卓真机 2 台
2. iPhone 真机 2 台
3. Wi-Fi / 4G 各测一轮
4. 至少 20 条真实抖音分享文案样本
5. 至少 5 条异常样本

## 不通过标准

只要出现下面任一项，就不要上线：

1. 解析成功率低于 90%
2. 真机保存成功率低于 85%
3. 下载失败没有明确错误提示
4. 相册权限拒绝后无法恢复
5. 高峰时接口没有限流保护
