# 万能下载器小程序实现方案

## 一句话结论

这个产品能做，但不能把核心逻辑放在微信小程序前端里。

正确做法是：

1. 小程序只负责贴链接、展示结果、触发下载
2. 后端负责解析抖音分享文案、跳转短链、抽取作品 ID、拿作品信息、生成可下载地址
3. 下载链路尽量走你自己的中转服务，不要指望小程序直接稳定下载抖音原始地址

这就是整个游戏。

## 目标用户

用户已经在抖音里复制了分享文案。

他打开你的小程序，只想做三件事：

1. 粘贴
2. 看到作品信息
3. 点一下保存

越像工具，越好。

## 你这版界面已经说明了什么

从截图看，产品的主路径已经比较明确：

1. 首页输入分享文案
2. 点击“开始提取”
3. 进入作品详情
4. 展示视频、图片、标题、清晰度、大小、多个源
5. 支持“复制链接”和“保存视频”

这条路径是对的。

真正难的不是页面，而是后面的解析服务稳定性。

## 先把边界定死

### 推荐 MVP 范围

只做：

1. 抖音公开视频分享文案解析
2. 单视频 / 单图集作品信息展示
3. 结果页下载或复制直链
4. 最近历史记录

### 明确不做

1. 不做私密视频、登录态内容、付费内容
2. 不做批量抓取
3. 不做“全平台万能”第一版
4. 不把“去水印”当作对外承诺能力
5. 不做纯前端解析

如果第一版就想做“任意平台、任意作品、永远稳定”，你会非常快进入反爬和规则对抗泥潭。那不是 MVP，那是给自己挖坑。

## 为什么一定要后端

微信小程序前端有三个现实限制：

1. 网络请求必须走已备案的 HTTPS 合法域名
2. 某些媒体直链需要特定请求头、跳转链路、时效签名，前端很难稳定处理
3. 解析抖音分享内容本质上是平台适配和反脆弱问题，必须能热修、限流、缓存、切换策略

所以架构上必须是“小程序薄前端，服务端厚能力”。

## 推荐架构

```text
┌─────────────────────┐
│ 微信小程序           │
│ 首页 / 详情 / 历史   │
└─────────┬───────────┘
          │ HTTPS API
          v
┌─────────────────────┐
│ API 网关 / 业务服务  │
│ 鉴权、限流、审计     │
└─────────┬───────────┘
          │
          ├───────────────┐
          │               │
          v               v
┌──────────────────┐  ┌──────────────────┐
│ 解析引擎          │  │ 下载中转服务      │
│ 文案抽取、短链展开 │  │ 代理下载、文件名   │
│ 作品ID识别         │  │ 过期签名、回源     │
└─────────┬────────┘  └─────────┬────────┘
          │                      │
          v                      v
┌──────────────────┐  ┌──────────────────┐
│ 缓存 / 数据库     │  │ 对象存储(可选)     │
│ Redis / MySQL     │  │ COS / OSS / MinIO │
└──────────────────┘  └──────────────────┘
```

## 前端实现

### 页面结构

建议四个页面就够：

1. `pages/index`
   输入框、粘贴按钮、开始提取、教程入口
2. `pages/result`
   作品标题、作者、封面、视频/图片 Tab、源切换、下载按钮
3. `pages/history`
   最近解析记录
4. `pages/profile`
   常见问题、免责声明、反馈

### 核心组件

1. `link-input`
   输入、清空、从剪贴板读取
2. `media-preview-card`
   封面、时长、清晰度、文件大小
3. `source-switcher`
   多源切换
4. `download-action-bar`
   复制链接、保存视频、保存图片
5. `parse-status`
   解析中、成功、失败、重试

### 小程序端状态机

不要把页面写成一坨 if-else。用状态机会稳很多。

```text
IDLE
  -> INPUT_READY
  -> PARSING
  -> PARSE_SUCCESS
  -> DOWNLOAD_PREPARING
  -> DOWNLOADING
  -> SAVE_SUCCESS
  -> ERROR
```

### 小程序端要做的事

1. 读取输入框或剪贴板文本
2. 调用 `/api/parse`
3. 渲染后端返回的标准化结果
4. 调用 `/api/download/prepare`
5. 使用 `wx.downloadFile`
6. 视频保存时调用 `wx.saveVideoToPhotosAlbum`
7. 图片保存时调用 `wx.saveImageToPhotosAlbum`
8. 做好相册权限申请和失败提示

## 后端实现

### 技术栈建议

推荐：

1. `Node.js + TypeScript + Fastify`
2. `undici` 或 `got` 处理 HTTP
3. `Redis` 做缓存、限流、短期结果存储
4. `MySQL` 做历史、用户记录、审计
5. `Playwright` 仅作为兜底策略，不要默认走浏览器

为什么选这套：

1. 解析服务更多是 IO 密集，不是重计算
2. TS 在接口建模和多平台适配器上很舒服
3. 以后你要扩“快手、小红书、B站”，适配器模式会很好用

### 后端核心模块

#### 1. 文案解析器

输入一整段分享文案，输出：

1. 平台
2. 短链 URL
3. 纯作品链接
4. 作品 ID

需要做的事：

1. 正则提取 URL
2. 清洗空格、换行、特殊字符
3. 识别 `v.douyin.com/...` 这类短链
4. 跟随 301/302 跳转
5. 从最终 URL 或 HTML 中提取作品 ID

#### 2. 平台适配器

建议定义统一接口：

```ts
interface PlatformAdapter {
  canHandle(input: string): boolean;
  resolveShareText(input: string): Promise<ResolvedShare>;
  fetchMetadata(resolved: ResolvedShare): Promise<MediaAsset>;
  prepareDownload(asset: MediaAsset, sourceId: string): Promise<DownloadTicket>;
}
```

第一版只有 `DouyinAdapter`。

以后要扩平台，直接新增适配器，不要到处写 `if (platform === 'douyin')`。

#### 3. 元数据标准化器

后端不应该把第三方返回原样吐给前端。

统一成你自己的结构：

```json
{
  "parseId": "p_20260429_xxx",
  "platform": "douyin",
  "type": "video",
  "title": "当同事天天作秀内卷",
  "author": {
    "name": "小野猫史",
    "avatar": "https://..."
  },
  "cover": "https://...",
  "durationMs": 31000,
  "sources": [
    {
      "id": "s1",
      "label": "1080P",
      "format": "mp4",
      "size": 2327831,
      "url": "https://...",
      "via": "proxy"
    }
  ],
  "images": [],
  "watermark": "unknown"
}
```

前端只认你的协议。别让前端知道第三方世界有多乱。

#### 4. 下载准备服务

不要让前端拿到不可控的原始地址后自己乱下。

建议先走一个 `prepare` 接口：

1. 校验结果是否过期
2. 校验源是否可用
3. 生成短时下载票据
4. 返回你的中转地址

比如：

`GET /api/download/file/:ticket`

由服务端回源拉取，再回传给小程序。

这样做的价值很大：

1. 可以隐藏真实上游地址
2. 可以统一文件名
3. 可以加风控
4. 上游规则变了，只改后端

#### 5. 历史记录模块

第一版别做太重。

保存：

1. 标题
2. 封面
3. 平台
4. 解析时间
5. 最近一次下载结果

可以按用户 OpenID 做 20 到 50 条最近记录就够了。

## 接口设计

### `POST /api/parse`

请求：

```json
{
  "text": "3.87 复制打开抖音，看看【...】 https://v.douyin.com/xxxx/"
}
```

返回：

```json
{
  "success": true,
  "data": {
    "parseId": "p_xxx",
    "platform": "douyin",
    "type": "video",
    "title": "作品标题",
    "author": { "name": "作者名", "avatar": "https://..." },
    "cover": "https://...",
    "durationMs": 31000,
    "sources": [
      {
        "id": "src_1",
        "label": "1080P",
        "size": 2327831,
        "format": "mp4"
      }
    ],
    "images": []
  }
}
```

### `POST /api/download/prepare`

请求：

```json
{
  "parseId": "p_xxx",
  "sourceId": "src_1"
}
```

返回：

```json
{
  "success": true,
  "data": {
    "ticket": "dl_xxx",
    "downloadUrl": "https://api.yourdomain.com/api/download/file/dl_xxx",
    "fileName": "当同事天天作秀内卷.mp4",
    "expiresAt": 1777777777
  }
}
```

### `GET /api/history`

返回当前用户最近记录。

### `POST /api/feedback`

收集“解析失败样本”，这会非常值钱。

## 抖音这块最现实的判断

### 能不能做

能。

### 难点在哪

不是“写个接口”这么简单。

难点在：

1. 分享文案格式会变
2. 短链跳转链路会变
3. 作品页结构会变
4. 上游直链可能带签名、时效、Referer、User-Agent 依赖
5. 会遇到风控、频控、验证码、地区限制

### 最重要的产品判断

如果你追求的是“稳定可用”，不要把“匿名直连抖音原链”当成唯一方案。

更稳的做法是：

1. 优先走你自己的后端中转
2. 对相同作品做短期缓存
3. 失败时给用户“复制下载链接”
4. 把解析过程做成可观测系统

## 如果你能走官方能力，边界会更清楚

根据抖音开放平台当前文档，存在“视频数据”能力，但它是基于授权体系给应用查询用户视频相关数据的能力，不等价于“匿名解析任意公开分享链接”。

这意味着：

1. 如果你拿的是用户授权数据，可以更规范
2. 如果你做的是“用户粘贴任意公开视频分享文案后立即解析”，通常还是要靠你自己的解析适配层

所以工程上别把“等官方开放 API”当主方案。

## 失败模式清单

| 场景 | 用户看到什么 | 根因 | 应对 |
|---|---|---|---|
| 分享文案里没有链接 | 解析失败 | 用户只贴了标题 | 前端先做 URL 检测 |
| 短链失效 | 无法提取 | 分享链接过期 | 返回“请重新复制抖音分享文案” |
| 作品下架或私密 | 无法查看作品 | 上游内容不可访问 | 返回明确文案，不要假装系统错误 |
| 解析成功但下载失败 | 点保存没反应 | 直链过期 / 防盗链 | 强制走中转下载 |
| 视频保存失败 | 下载完成但未入相册 | 权限拒绝 | 引导开启相册权限 |
| 文件过大或网络慢 | 一直转圈 | 客户端下载耗时 | 加超时、进度条、失败重试 |
| 突发大量请求 | 全站变慢 | 被刷接口 | OpenID + IP 双限流 |
| 上游结构变更 | 一批链接全失效 | 适配器老化 | 做监控和失败样本回收 |

## 安全和合规

这部分不能糊弄。

至少要做：

1. 明确声明仅处理用户主动提交的公开分享内容
2. 明确声明用户应保证自己对下载和使用行为拥有合法权限
3. 不保存完整原始分享文案过长时间，建议只保存必要字段和哈希
4. 对下载接口做签名、限频、过期控制
5. 对异常高频用户做封禁

如果你准备商业化，这块最好找律师看一下平台规则和落地文案。

## 你最该避免的坑

### 坑 1，纯小程序直连解析

看起来省事，实际上很脆。

### 坑 2，前端直接保存第三方原始下载地址

你会遇到过期、失效、跨域、Referer、防盗链问题。

### 坑 3，第一版就追求“全平台”

你会把整个系统写成垃圾场。

### 坑 4，没做失败回收

解析类产品的核心资产不是页面，是失败样本。

你每次失败都应该知道：

1. 失败的分享文案长什么样
2. 失败在哪一步
3. 是否可复现
4. 是否需要新增规则

## 推荐开发顺序

### Phase 1，先打通最短链路

1. 首页输入
2. 后端解析抖音分享文案
3. 返回标题、封面、作者、一个可下载源
4. 小程序完成保存

做到这四步，产品就活了。

### Phase 2，做稳定性

1. 缓存
2. 限流
3. 错误分类
4. 历史记录
5. 失败样本回收

### Phase 3，再做体验

1. 多源切换
2. 图集支持
3. 下载进度
4. 更干净的结果页
5. 教程和 FAQ

## 我给你的技术决策

### 自动决策

1. 前端必须薄，后端必须重
2. 第一版只支持抖音
3. 必须设计平台适配器层
4. 下载必须经过 `prepare` + 中转
5. 必须做失败日志和缓存

### 味道决策

1. 后端框架用 `Fastify` 还是 `NestJS`
   我更推荐 `Fastify`，更轻，更适合工具型服务
2. 历史记录先存本地还是服务端
   如果你后面要登录态和风控，建议直接服务端

## What already exists

从截图能确认你已有的产品结构认知是对的：

1. 有首页入口
2. 有输入框和“开始提取”
3. 有作品详情页
4. 有视频预览、清晰度、文件大小、多源切换
5. 有复制链接和保存视频动作

所以这不是从 0 想创意，而是把后端能力补齐。

## NOT in scope

这次方案不包含：

1. 抖音外平台的详细适配规则
2. 真实线上爬虫策略代码
3. 规避平台风控的对抗实现
4. 商业投放、广告、分销设计

## Dream state delta

你现在离“可上线工具”的差距，不是 UI，而是这四个能力：

1. 稳定解析
2. 稳定下载
3. 稳定失败处理
4. 稳定运维观测

把这四个补上，这东西就不是 demo 了。

## Cross-phase themes

跨产品、工程、体验三个视角，反复出现的是同一件事：

这个产品的护城河不是页面，也不是按钮多绿。

是解析链路的稳定性和异常恢复能力。

## Decision Audit Trail

| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
|---|---|---|---|---|---|---|
| 1 | CEO | 第一版只支持抖音 | auto | 聚焦 | 缩小变量，先证明主链路 | 全平台首发 |
| 2 | CEO | 不承诺纯前端实现 | auto | 现实约束 | 小程序端不适合承载解析核心 | 纯小程序直连 |
| 3 | Eng | 引入平台适配器接口 | auto | 可演进 | 后续扩平台不污染业务层 | 各处散落 if-else |
| 4 | Eng | 下载先 prepare 再中转 | auto | 稳定性 | 便于鉴权、隐藏上游、统一回源 | 前端直下第三方原链 |
| 5 | Eng | Playwright 仅做兜底 | auto | 简单优先 | 浏览器方案成本高、维护重 | 默认浏览器抓取 |
| 6 | DX | 首页只保留单一核心 CTA | auto | 降低认知负担 | 工具类产品要快，不要杂 | 首页堆更多入口 |
| 7 | Security | 保存必要字段和哈希 | auto | 最小化留存 | 降低合规和数据泄露压力 | 长期存原始分享文案 |

